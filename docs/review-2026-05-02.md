# Egypt Deals — Review (2026-05-02)

## 1. Summary (TL;DR)

The app is in solid shape for a personal PWA. Frontend builds clean, scraper pulled 184 deals (30 Amazon + 154 Noon) without errors, push delivery works end-to-end, the deal-detail price assessment is genuinely useful, and the live site at https://egypt-deals.vercel.app renders well at iPhone width. This pass landed 7 fixes (one of them caught only by smoke-test on the live deploy) plus an a11y/copy polish bundle. The most visible improvements are the skeleton grid + stale-data banner on first load, properly-clearing dead push subscriptions, and a real "Newest" sort. Biggest open question: the `priceCount >= 1` ATL threshold is intentionally loose but means most "ATL" claims early on are weak — needs a product call.

## 2. Bugs found and fixed

- **"Newest" sort was a no-op** — `scraper/index.js:62` sets `scrapedAt = Date.now()` for every deal in the run, so sorting on it never reordered anything. Switched to `firstSeenTs` (per-product earliest record) — commit `9d1a32b`. Users would have noticed the Newest sort showed the same order as whatever sort they had before.
- **Service-worker offline fallback was dead** — `public/sw.js` intercepted navigation with `caches.match('/')` but nothing ever populated the cache, so offline navigation rejected silently with `undefined`. Removed the handler so the browser uses its native cache + offline page. Commit `cb4ccf3`. Users on poor connectivity would have seen the PWA fail to load entirely instead of getting a useful error.
- **SW default icon path mismatch** — pushed payload without an explicit `icon` fell back to the pre-cache-bust `/icons/icon-192.png`. Bumped to `-v3.png`. Commit `cb4ccf3`. Notification icon could have been stale.
- **Stale push subscription never cleared** — `scraper/push.js` logged on 404/410 but kept the dead endpoint in Upstash, so every subsequent scrape retried it. Now we `redis.del('push_subscription')` on those status codes. Commit `b8f91b7`. After uninstalling the PWA the user would see persistent push errors in the scraper logs and re-subscribing cleanly required manual Redis intervention.
- **Single-deal notification icon path inconsistency** — single-deal pushes used `/icons/icon-192.png`, digest used `-v3.png`. Unified to `-v3.png`. Commit `b8f91b7`.
- **Header showed "Loading" forever after empty fetch** — `/api/deals` returned `updatedAt: null` but the header bound `snapshot?.updatedAt ? timeAgo(...) : 'Loading'`. API now returns `0`, header now shows "No data yet". Commit `f2c8cdb`. First-ever cold start (before any scrape) would have looked broken.
- **Amazon deep-link double-opened web tab** — `lib/deep-link.ts` opened the web fallback unconditionally 500ms after the `amazon://` scheme. Now checks `document.hidden` so the fallback is suppressed when the app handled it. Commit `526309d`. Users with the Amazon app installed would have seen both the app open AND a stale browser tab linger.
- **Settings "1 minutes ago" pluralization** — caught on live smoke test. Commit `1deb773`.

## 3. Bugs found but NOT fixed

- ~~**`isAllTimeLow` returns true at `priceCount >= 1`**~~ — **RESOLVED in commit `bf391f8`**. Threshold tightened to `priceCount >= 5`. After the change the live snapshot still produced 50 ATL deals (we already had ~5h of accumulated history).
- ~~**"Near the lowest seen — within EGP 0 (0-day window)"**~~ — **RESOLVED in commit `e65a813`**. New `flat` verdict tone catches `range === 0 && priceCount >= 3` with copy "Price has been flat at EGP X across N samples over Nd."
- ~~**Smart-verdict "Price is volatile" can mislead**~~ — **RESOLVED in commit `d698334`**. Now uses coefficient of variation (>5%) and adds a "Price has been stable" note for low-CV cases.
- **`scrapedAt` field is uniform per run** — see §6.
- **No `.gitignore` for `tsconfig.tsbuildinfo` and `scraper/data/`** — both got tracked accidentally during this session. Worth adding but not user-visible.

## 4. Polish applied (single commit `e7ef7eb`)

- Comma-separated prices everywhere: `EGP 12,999` instead of `EGP 12999`, via a shared `fmtEGP` helper in `DealCard.tsx` and `app/deal/[id]/page.tsx`.
- `aria-pressed` reflects active state on filter chips; `role="tablist"` on each row; `aria-label`s ("Filter by store", "Filter by category").
- `focus-visible:ring-2 focus-visible:ring-blue-500` on deal cards and filter chips so keyboard users see focus.
- DealCard `aria-label` includes name + price + discount so screen readers don't read raw "EGP 12999" as a single number.
- Settings push toggle: when `Notification.permission === 'denied'` the toggle is disabled with helper copy "Blocked — re-enable in iOS Settings → Notifications → Deals". Previously it just `alert()`ed something useless on tap.
- Deal detail: hide the "Smart verdict" block when `priceCount < 3` (the price-assessment block above already covers the not-enough-data case, and the smart verdict at <3 points is generic).
- Skeleton grid (6 pulsing cards) on first load instead of a blank page with "Loading" in the header.
- Stale-data amber banner when `Date.now() - updatedAt > 3h`.

## 5. What's missing — recommended next

### MUST

- **Product call: tighten ATL threshold (S)** — should `isAllTimeLow` require `priceCount >= 5` (or some confidence floor) before stamping the green "ATL (our data)" badge? Today every product is "ATL" on first scrape. **Question for Ahmed:** OK to gate ATL on `priceCount >= 5`? It will cut the feed empty for ~5 hours after a fresh DB reset but make every shown deal trustworthy.
- **Edge-case verdict copy: handle "all samples identical" (S)** — `assessPrice()` should detect `range === 0 && priceCount >= 3` and print something honest like "Price has been flat at EGP X for {days}d". The current "Within EGP 0 of the all-time low (0-day window)" reads as broken UX. User-visible win: the most-clicked detail view stops looking janky.

### SHOULD

- **Empty-image fallback (S)** — `DealCard` renders the 📦 emoji when `imageUrl` is empty (e.g. the men's socks card on the live site). Either skip such items in the snapshot, or render a more graceful generic image. Win: feed feels finished, not buggy.
- **Manual scrape spinner accuracy (M)** — `app/page.tsx` polls `/api/deals` 18× over 90s waiting for `updatedAt > before`. If the user's PC is off, this spins for 90s then silently gives up with no feedback. Add a "couldn't reach scraper — your PC may be off" toast on timeout. Win: trust.
- **Settings: scraper health card (S)** — show actual `last_scrape_ts` (we already store it!) plus next expected run estimate. Currently "Last updated" piggybacks on the snapshot timestamp. Win: clearer mental model when push fails.
- **Wishlist / saved deals (M)** — let the user star a product and get notified specifically when *that* product hits a new low, even if it's not in the top-50 ATL feed. Win: the feature people actually use price trackers for.
- **iOS PWA test pass (S)** — verified at desktop-emulated iPhone width via Chrome MCP, but Ahmed should install on iPhone Home Screen and confirm the safe-area inset, the touch refresh, the swipe-back gesture, and most importantly that a delivered push tapped from lock screen actually navigates to `/deal/<id>`.
- **Search debouncing + history (S)** — search currently re-filters every keystroke (cheap, but no analytics on what was searched). Persist last 5 searches to localStorage and surface as chips. Win: faster repeat lookups.
- **Tighten "Smart verdict" volatility heuristic (S)** — see §3. Compute stdev relative to mean; only call "volatile" when stdev/mean > ~5%.

### COULD

- **Per-deal "watch this" → personal price target alert (M)** — extension of wishlist; user enters a target price, gets a push when it crosses.
- **Scraper failure visibility (S)** — currently a 0-deals scrape result silently overwrites the snapshot with an empty deals array. Either skip the snapshot write on `allDeals.length === 0` or show "Last scrape failed" in settings.
- **Batch-screenshot share image (S)** — long-press a deal → generate a price-history PNG to share to friends. Pure nice-to-have.
- **Categorize "general" / "other" buckets (S)** — current classifier sends ~30% of products to `other`. Look at what's landing there for a week and tighten rules. Win: filters feel exhaustive.
- **PWA install prompt (S)** — first visit on iOS Safari, show a one-time tip explaining the Share → Add to Home Screen flow. Win: more push subscribers.
- **Dark/light theme toggle (M)** — the app is fully dark; iPhone OLED users will love it but a light mode for outdoors visibility wouldn't hurt.

## 6. Architecture notes

- **`scrapedAt` is uniform across a scrape run.** It's set to `Date.now()` for every deal in `scraper/index.js:62` so it's effectively just a "snapshot timestamp" duplicated onto every record. The frontend type `Deal.scrapedAt` invites confusion. Either drop it from the per-deal payload (use `DealsSnapshot.updatedAt` instead) or genuinely set it from the SQLite row's `MAX(scrapedAt)`.
- **`getSecondLowestPrice` returns the *lowest* price when only one distinct value exists** (`db.js:81`), not null. Consumers in `index.js` treat it as "previous low," which then equals current price, which makes the verdict ratio `1.0` → falls into the "matches previous all-time low" branch. This is OK in practice but a subtle naming lie.
- **`upsertProduct` writes the `imageUrl` it just scraped on every run.** If a category page shows a different thumbnail size for the same product on a later run, the URL flickers. Probably fine; flagging for awareness.
- **No idempotency on `setDealsSnapshot`.** Two simultaneous scrapes (poller + manual + scheduled) could race and one's snapshot would clobber the other. Today only one scraper runs at a time (the poller has `inFlight` guard) but if `runScrape()` is ever invoked from two scripts the snapshot becomes whichever finishes second.
- **`@upstash/redis` is duplicated in both `lib/upstash.ts` (frontend) and `scraper/upstash.js`** — that's fine for keeping client-only and node-only code separate, but the new `clearDeadSubscription` in `scraper/push.js` instantiates a *third* Redis client. Could refactor to share a single accessor inside `scraper/`.
- **Type `Deal` lives only in `lib/types.ts`** — scraper-built objects aren't typed. If scraper ever drops a field the frontend breaks at runtime. A shared `.d.ts` or runtime validator (zod) would help but is overkill for a personal app.
- **Service worker has no versioning or update strategy.** It calls `skipWaiting()` + `clients.claim()` on every install, which is correct for a PWA where you want freshness over freshness-control. Just noting.
- **`tsconfig.tsbuildinfo`, `scraper/data/`, and the captured `amazon-debug.html` got committed during this session.** Should be in `.gitignore`. Out of scope for this pass.

## 7. Verification log

What I actually did:

1. `npm run build` (Next 16.2.4 / Turbopack) — passed clean before any change.
2. `npm run build` again after every code change — passed clean each time.
3. `cd scraper && node index.js` — ran a real scrape after the `push.js` rewrite. Result: 30 Amazon + 154 Noon = 184 total, 50 ATL uploaded, digest push delivered to live subscription, no errors. Confirmed the `clearDeadSubscription` import didn't break anything (the push succeeded so the failure path didn't trigger, but the require-graph resolved).
4. Chrome MCP attached to Ahmed's local Chrome, resized to 420×900 (close to iPhone 14 viewport but with desktop pixel density).
5. Loaded `https://egypt-deals.vercel.app/` — confirmed the 2-column grid renders, NEW + discount badges visible, store dot (N) on Noon cards, "just now" timestamp matched the scrape I had just run.
6. Tapped a deal card → routed to `/deal/noon-N70006744V`. Confirmed price-quality verdict block, LOW/AVG/HIGH/TRACKED grid, price chart, "Smart verdict" block (which surfaced the §3 edge case), Open on Noon / Compare / share buttons.
7. Navigated to `/settings` — confirmed "Last updated 1 minutes ago" pluralization bug (fixed and shipped before this report).
8. `read_console_messages` filtered for error/warn/fail — no JS errors on either page after fresh load.
9. Spot-checked `app/page.tsx`, `app/deal/[id]/page.tsx`, `app/settings/page.tsx`, all of `components/`, all of `lib/`, the `scraper/` directory, `public/sw.js`, and the API routes.

What I deliberately did NOT do:
- Did not run an iPhone-width *actual* mobile test (no physical device in this session). Desktop emulation at 420px is a good proxy but not the same — flagged for Ahmed in §5 SHOULD.
- Did not exercise the Run-Scrape-Now button on settings (would have triggered another scrape and clobbered the digest-push de-dup data).
- Did not test push notification rendering on iOS (would require physical device install).
- Did not test pull-to-refresh gesture (Chrome MCP can't simulate iOS touch flick).

## 8. Backlog execution (2026-05-02 evening)

Two MUSTs from §5 plus the entire SHOULD list landed. Each as a separate commit.

| # | Item | Commit | What the user notices |
|---|---|---|---|
| 1 | Tighten ATL threshold to `priceCount >= 5` | `bf391f8` | Feed only shows products with ≥5 hourly samples — ATL badge stops being trivially true. Live scrape after the change produced 50 ATL deals (history was already deep enough). |
| 2 | Flat-price verdict edge case | `e65a813` | Detail page no longer prints "Within EGP 0 of the all-time low (0-day window)". Replaced by a new `flat` tone: "Price has been flat at EGP X across N samples over Nd." |
| 3 | Settings scraper-health card | `957b748` | New `/api/last-scrape` endpoint plus a four-line card on Settings: Last scrape · Next run (~minutes) · Snapshot updated · Deals tracked. Distinguishes "scraper alive" from "snapshot fresh" when a zero-deals run happens. |
| 4 | Manual refresh failure toast | `fb2e68c` | When the 18×5s poll loop expires without `updatedAt` advancing, a transient red banner says "Couldn't reach scraper — your PC may be off". Auto-dismisses after 6s. |
| 5 | Empty-image fallback | `7c66020` | Cards without an `imageUrl` now show a slate gradient with the first 2 alphanumeric chars of the product name in big bold type instead of the 📦 emoji. |
| 6 | Search history chips | `341bbb5` | Last 5 searches persist to `deals_search_history_v1`. Tappable chips appear below the search input, MRU-first, dismissible. Recorded on Enter, blur, or clear-X. |
| 7 | Smart-verdict volatility heuristic | `d698334` | "Price is volatile" now requires coefficient of variation > 5% (stdev/mean). Otherwise reads "Price has been stable". 1-EGP wiggles on a 999-EGP item no longer scare the user. |
| 8 | Wishlist with push delivery | `70d8302` | Star button on every DealCard. New `/saved` page accessible from the home header (★) and settings header (★ Saved). Stars mirror to Upstash via `/api/wishlist`. Scraper checks saved IDs each run; when a watched product hits or beats its saved-when price AND drops further than the last alert, sends a "★ <name>" push. SAVED_NOTIFIED hash prevents re-spamming. |

### Verification log (this pass)

- `npm run build` clean after every commit.
- `cd scraper && node index.js` after the threshold/volatility/wishlist changes — 184 deals, 50 ATL uploaded, 0 push errors, "0 saved-deal pushes" log line confirms the new code path executed.
- Chrome MCP: navigated `/`, programmatically clicked a star button, confirmed `localStorage.deals_wishlist_v1` populated, navigated `/saved` and confirmed the deal renders with name + saved date + price. Settings card displayed "Last scrape: 1 minute ago / Next run: in ~58 minutes / Snapshot updated: 1 minute ago / Deals tracked: 50". Search history chip rendered after a programmatic Enter press on the search input. Cleaned up test data (unsaved the deal locally + via API) before finishing.
- Manual-refresh failure toast NOT live-tested (would require simulating a scraper crash). The code path is straightforward: `advanced` flag flips false on poll-loop exhaustion → banner renders → 6s setTimeout hides it.

### For Ahmed to verify on iPhone

- Star a deal from the home feed — confirm the yellow star turns on and persists across an app relaunch (not just reload).
- Tap ★ in the header → confirm `/saved` lists the starred deal.
- Tap a saved item → opens the deal detail page.
- Optional smoke-test: drop a saved product's `savedAtPrice` artificially via DevTools so the next scrape pushes you. (Or just wait for a real price drop — the channel is wired the same way as the digest, so if the digest reaches your lock screen, the wishlist push will too.)
- Settings → confirm the new four-line scraper-health card numbers update after a "Run Scrape Now" press.

## 9. Long-term learning system (2026-05-02 late evening)

The user's ask: *"i want the app to keep learning about the deals the more days pass the more data in the history to be able to know if this real deal or not."* This pass shipped the architecture for that.

### Two gaps closed

**Coverage** — `scraper/watcher.js` re-fetches up to 12 stale products per scrape run, oldest `products.updatedAt` first, filtered to `priceCount >= 3` and last seen within 90 days. It fetches Amazon and Noon PDPs by raw HTTP (Cloudflare-friendly), parses with three fallback strategies (aok-offscreen → a-price-whole+fraction → legacy a-offscreen for Amazon; data-qa price-now for Noon). Sanity bounds reject prices outside `[0.1× existing min, 5× existing max]` so a parser drift can't poison history. Out-of-stock and captcha pages are detected and skipped (counted, not re-tried mid-run). The watcher writes only to `price_history` — it never touches the user-facing snapshot, which remains ATL-only from the main scrape.

**Signal quality** — three new scalars per snapshot deal, computed from the *full* price history (not the 30-point chart slice):
- `percentile` — fraction of recorded samples this current price beats, midrank tie-handling (strict-greater + 0.5 × ties).
- `runLength` — trailing samples within ±0.5% of current. Tells "this price has held steady for N samples."
- `freqAtOrBelow` — count of samples ≤ current. Tells "seen at or below EGP X only N of M times."

### Detail-page verdict tiers (new)

`assessConfidence` and `assessPrice` now share thresholds — they used to disagree. The verdict copy depends on confidence:

- **Limited** (priceCount < 5 OR days < 2) — falls back to position-in-range copy with explicit hedging: "we've only tracked 0d." Honest about thin data.
- **Moderate** (priceCount ≥ 5 AND days ≥ 2) — uses the percentile but hedges: "Beats 76% of the 21 prices recorded so far. Verdict will sharpen as we collect more."
- **Strong** (priceCount ≥ 14 AND days ≥ 7) — full bold framing: "Beats 91% of 47 recorded prices for this product. Seen at or below EGP 4,500 only 4 times."

The detail page also shows a new learning row under the chart (PERCENTILE / RUN / SEEN ≤) when priceCount ≥ 5, so the underlying numbers stay visible.

### What Ahmed sees as history grows

- **Day 1** (now): "Limited data" everywhere. Verdicts say "we've only tracked 0d" or "Just started tracking — verdict sharpens as days pass."
- **Day 3** (~72 hourly samples per always-listed product, ~24 per watched-only product): Most products move into "Moderate signal." Copy reads "Beats 76% of 21 recorded prices. Promising but not deep history yet."
- **Day 14** (~336 samples per always-listed product): Most ATL products in "Strong signal." Copy reads "Beats 91% of 336 prices. Seen at or below EGP 4,500 only 4 times. Has held near EGP 4,500 for the last 12 samples."

### Retention and growth

No deletion — `price_history` rows are kept forever. SQLite handles this fine. Back-of-envelope: ~470 products × hourly × 30d ≈ 340k rows; Ahmed's local SQLite handles tens of millions. Re-evaluate at 6+ months if the file size feels heavy.

### Data hygiene risks

- **Amazon URL drift** — `/deals` cards include tracking params (`pf_rd_r`, `pf_rd_p`, etc.) that change each run. `upsertProduct` overwrites the URL on every match, so the canonical URL drifts run-to-run. Not a *correctness* problem — keying is on `amz-<ASIN>`, which is stable, so we never split history. The watcher fetches via the latest stored URL; Amazon happily redirects.
- **Noon ID** is `N<digits>V` — stable, no drift risk.
- **The `seenAtThisPrice` calc still uses exact equality** in `index.js`. With float prices that's mostly fine but can underestimate "rarity." Consider migrating to the `runLength` band logic in a follow-up.

### Verification

- `npm test` — 23 tests pass (4 new in `db.test.js` covering percentile / run-length / freq / watcher-pool selection; `atl.test.js` updated to match the priceCount ≥ 5 threshold).
- `npm run build` — clean.
- Live scrape: 184 deals (30 Amazon + 154 Noon), 50 ATL uploaded with new fields populated. Watcher: 8 OK, 4 out-of-stock skips, 0 fail.
- Live URL `/deal/noon-N70006744V` (flat-price product, 21 samples, all at EGP 122) → "Price has been flat" verdict + `PERCENTILE 50% / RUN 21 samples / SEEN ≤ 21/21`.
- Live URL `/deal/noon-N70033897V` (real variance, percentile 76%) → "Lowest in our short window — Down EGP 28 from the highest of 21 samples — but we've only tracked 0d" + `PERCENTILE 76% / RUN 10 / SEEN ≤ 10/21`.
